# Required to CREATE TABLE / fix RLS (Project Settings → Database → Database password)
SUPABASE_DB_PASSWORD=your-database-password

# Optional: full connection string instead of password above
# DATABASE_URL=postgresql://postgres:PASSWORD@db.bjzhmdpuzfbpkvohntjx.supabase.co:5432/postgres

# Optional: bypass RLS for seeding only (Project Settings → API → service_role key)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: Supabase Management API personal access token
# SUPABASE_ACCESS_TOKEN=your-access-token
